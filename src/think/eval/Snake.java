package think.eval;

import java.util.ArrayList;
import java.util.HashSet;
import think.repr.Board;
import think.repr.Point;

public class Snake {

    public static ArrayList<Point> bfs(
        Board board,
        HashSet<Point> rubbers,
        HashSet<Point> destinations,
        Point source
    ) {
        throw new UnsupportedOperationException();
    }
}
